import React from 'react';
import { ProviderProps } from '.';
interface ContextProps {
    isOpen?: boolean;
    handleOpen?: () => void;
    handleClose?: () => void;
    randomId: string;
}
export declare const AppContext: React.Context<ContextProps>;
export declare function OffcanvasProvider({ children, onOpen, onClose }: ProviderProps): JSX.Element;
export {};
